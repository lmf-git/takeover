{
  "name": "takeover",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node core/server/index.js",
    "build": "node core/server/build.js",
    "start": "NODE_ENV=production node core/server/index.js",
    "deploy:cloudflare": "yarn build && cp deploy/cloudflare/_worker.js dist/client/_worker.js && wrangler pages deploy"
  }
}
